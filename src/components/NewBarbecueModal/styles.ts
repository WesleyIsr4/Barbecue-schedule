import styled from 'styled-components';

export const Container = styled.div`
  h2 {
    margin-bottom: 2rem;
    font-size: 1rem;
    color: var(--title);
  }

  .input-group {
    display: flex;
    gap: 1rem;

    margin: 1rem 0 2rem 0;

    div {
      margin: 0;
    }
  }
`;
